(() => {
  let productTitle = 'my product';
  productTitle = 'product change'
  console.log(productTitle);

  const sumary = `
  title: ${productTitle}
  descrip: ${productTitle}
  `;

  console.log(sumary);

  
})()
